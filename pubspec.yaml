name: pkgs
description: An experimental tool to manage mono-repos.
version: 0.1.0
repository: https://github.com/devoncarew/pkgs

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.0
  glob: ^2.1.0
  path: ^1.8.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.0
